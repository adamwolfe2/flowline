export const metadata = {
  title: "Sign Up | MyVSL",
  description: "Create your MyVSL account",
};

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
