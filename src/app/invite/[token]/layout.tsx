export const metadata = {
  title: "Accept Invite | MyVSL",
  description: "Accept your team invitation to MyVSL",
};

export default function InviteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
