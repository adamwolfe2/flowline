export const metadata = {
  title: "Sign In | MyVSL",
  description: "Sign in to your MyVSL account",
};

export default function SignInLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
